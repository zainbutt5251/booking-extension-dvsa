var u = [
    { id: "10248", name: "Aberdeen North" },
    { id: "337", name: "Aberdeen North LGV" },
    { id: "1878", name: "Aberdeen South (Cove)" },
    { id: "247", name: "Aberfeldy" },
    { id: "355", name: "Abergavenny" },
    { id: "356", name: "Aberystwyth (Park Avenue)" },
    { id: "254", name: "Airdrie" },
    { id: "696", name: "Alness" },
    { id: "150", name: "Alnwick" },
    { id: "318", name: "Arbroath" },
    { id: "3459", name: "Ashfield" },
    { id: "94", name: "Ashford (Kent)" },
    { id: "1858", name: "Atherton (Manchester)" },
    { id: "42", name: "Aylesbury" },
    { id: "3679", name: "Ayr" },
    { id: "358", name: "Bala" },
    { id: "251", name: "Ballater" },
    { id: "43", name: "Banbury" },
    { id: "242", name: "Banff" },
    { id: "1318", name: "Bangor" },
    { id: "96", name: "Barking (Tanner Street)" },
    { id: "97", name: "Barnet (London)" },
    { id: "171", name: "Barnsley" },
    { id: "360", name: "Barnstaple" },
    { id: "244", name: "Barra" },
    { id: "159", name: "Barrow In Furness" },
    { id: "3985", name: "Barry" },
    { id: "1396", name: "Basildon" },
    { id: "362", name: "Basingstoke" },
    { id: "1416", name: "Bedford" },
    { id: "98", name: "Belvedere (London)" },
    { id: "245", name: "Benbecula Island" },
    { id: "158", name: "Berwick-On-Tweed" },
    { id: "428", name: "Beverley LGV" },
    { id: "1176", name: "Birmingham (Garretts Green)" },
    { id: "22", name: "Birmingham (Kings Heath)" },
    { id: "32", name: "Birmingham (Kingstanding)" },
    { id: "35", name: "Birmingham (Shirley)" },
    { id: "38", name: "Birmingham (South Yardley)" },
    { id: "5585", name: "Bishopbriggs" },
    { id: "17", name: "Bishops Stortford" },
    { id: "1860", name: "Blackburn with Darwen" },
    { id: "161", name: "Blackpool" },
    { id: "44", name: "Bletchley" },
    { id: "151", name: "Blyth" },
    { id: "363", name: "Bodmin" },
    { id: "197", name: "Bolton (Manchester)" },
    { id: "796", name: "Borehamwood (London)" },
    { id: "48", name: "Boston" },
    { id: "183", name: "Bradford (Heaton)" },
    { id: "3742", name: "Bradford (Thornbury)" },
    { id: "365", name: "Brecon" },
    { id: "3715", name: "Bredbury (Manchester)" },
    { id: "100", name: "Brentwood (London)" },
    { id: "366", name: "Bridgend" },
    { id: "175", name: "Bridlington" },
    { id: "6305", name: "Bristol (Avonmouth)" },
    { id: "1357", name: "Bristol (Kingswood)" },
    { id: "246", name: "Brodick (Isle of Arran)" },
    { id: "7805", name: "Bromley (London)" },
    { id: "250", name: "Buckie" },
    { id: "1258", name: "Burgess Hill" },
    { id: "1136", name: "Burton on Trent" },
    { id: "198", name: "Bury (Manchester)" },
    { id: "75", name: "Bury St Edmunds" },
    { id: "216", name: "Buxton" },
    { id: "456", name: "Callander" },
    { id: "372", name: "Camborne" },
    { id: "12", name: "Cambridge (Brookmount Court)" },
    { id: "1256", name: "Cambridge (Hardwick)" },
    { id: "260", name: "Campbeltown" },
    { id: "103", name: "Canterbury" },
    { id: "4585", name: "Cardiff (Llanishen)" },
    { id: "375", name: "Cardigan" },
    { id: "1276", name: "Carlisle" },
    { id: "225", name: "Carlisle LGV (Cars)" },
    { id: "376", name: "Carmarthen" },
    { id: "255", name: "Castle Douglas" },
    { id: "6505", name: "Chadderton" },
    { id: "199", name: "Cheetham Hill (Manchester)" },
    { id: "91", name: "Chelmsford (Hanbury Road)" },
    { id: "377", name: "Cheltenham" },
    { id: "776", name: "Chertsey (London)" },
    { id: "1257", name: "Chester" },
    { id: "56", name: "Chesterfield" },
    { id: "104", name: "Chichester" },
    { id: "105", name: "Chingford (London)" },
    { id: "378", name: "Chippenham" },
    { id: "200", name: "Chorley" },
    { id: "14", name: "Clacton-on-Sea" },
    { id: "15", name: "Colchester" },
    { id: "656", name: "Coventry" },
    { id: "106", name: "Crawley" },
    { id: "213", name: "Crewe" },
    { id: "259", name: "Crieff" },
    { id: "92", name: "Culham LGV" },
    { id: "256", name: "Cumnock" },
    { id: "958", name: "Darlington" },
    { id: "5685", name: "Derby (Alvaston)" },
    { id: "190", name: "Doncaster" },
    { id: "3659", name: "Dorchester" },
    { id: "344", name: "Dreghorn LGV" },
    { id: "24", name: "Dudley" },
    { id: "262", name: "Dumbarton" },
    { id: "265", name: "Dumfries" },
    { id: "340", name: "Dumfries LGV" },
    { id: "3802", name: "Dundee" },
    { id: "7685", name: "Dunfermline (Vine)" },
    { id: "266", name: "Dunoon" },
    { id: "267", name: "Duns" },
    { id: "3717", name: "Durham" },
    { id: "228", name: "Durham LGV" },
    { id: "5165", name: "East Kilbride" },
    { id: "108", name: "Eastbourne" },
    { id: "716", name: "Edinburgh (Currie)" },
    { id: "896", name: "Edinburgh (Musselburgh)" },
    { id: "269", name: "Elgin" },
    { id: "444", name: "Enfield (Brancroft Way)" },
    { id: "996", name: "Enfield (Innova Business Park)" },
    { id: "1056", name: "Erith (London)" },
    { id: "1016", name: "Exeter" },
    { id: "439", name: "Exeter LGV" },
    { id: "1336", name: "Farnborough" },
    { id: "88", name: "Featherstone" },
    { id: "109", name: "Folkestone" },
    { id: "276", name: "Forfar" },
    { id: "277", name: "Fort William" },
    { id: "274", name: "Fraserburgh" },
    { id: "281", name: "Gairloch" },
    { id: "279", name: "Galashiels" },
    { id: "957", name: "Gateshead" },
    { id: "3619", name: "Gillingham" },
    { id: "450", name: "Gillingham LGV" },
    { id: "280", name: "Girvan" },
    { id: "278", name: "Glasgow (Anniesland)" },
    { id: "676", name: "Glasgow (Baillieston)" },
    { id: "936", name: "Glasgow (Shieldhall)" },
    { id: "816", name: "Gloucester" },
    { id: "283", name: "Golspie" },
    { id: "482", name: "Goodmayes (London)" },
    { id: "232", name: "Gosforth" },
    { id: "4505", name: "Grangemouth" },
    { id: "5705", name: "Grantham (Somerby)" },
    { id: "285", name: "Grantown-On-Spey" },
    { id: "113", name: "Greenford (Horsenden Lane)" },
    { id: "3782", name: "Greenham" },
    { id: "282", name: "Greenock" },
    { id: "234", name: "Grimsby Coldwater" },
    { id: "115", name: "Guildford" },
    { id: "288", name: "Haddington" },
    { id: "184", name: "Halifax" },
    { id: "290", name: "Hamilton" },
    { id: "229", name: "Hartlepool" },
    { id: "458", name: "Hastings (Ore)" },
    { id: "293", name: "Hawick" },
    { id: "191", name: "Heckmondwike" },
    { id: "2018", name: "Hendon (London)" },
    { id: "20", name: "Hereford" },
    { id: "1337", name: "Herne Bay" },
    { id: "153", name: "Hexham" },
    { id: "163", name: "Heysham" },
    { id: "9628", name: "High Wycombe" },
    { id: "65", name: "Hinckley" },
    { id: "121", name: "Hornchurch (London)" },
    { id: "185", name: "Horsforth" },
    { id: "186", name: "Huddersfield" },
    { id: "976", name: "Hull" },
    { id: "292", name: "Huntly" },
    { id: "333", name: "Inveraray" },
    { id: "1237", name: "Inverness (Longman Drive)" },
    { id: "350", name: "Inverness (Seafield Road)" },
    { id: "294", name: "Inverurie" },
    { id: "76", name: "Ipswich" },
    { id: "3965", name: "Irvine" },
    { id: "305", name: "Islay Island" },
    { id: "308", name: "Isle of Mull" },
    { id: "324", name: "Isle of Skye (Portree)" },
    { id: "330", name: "Isle of Tiree" },
    { id: "390", name: "Isles of Scilly" },
    { id: "124", name: "Isleworth (Fleming Way)" },
    { id: "300", name: "Kelso" },
    { id: "5845", name: "Kendal (Oxenholme Road)" },
    { id: "1238", name: "Kettering" },
    { id: "1156", name: "Kings Lynn" },
    { id: "295", name: "Kingussie" },
    { id: "1196", name: "Kirkcaldy" },
    { id: "241", name: "Kirkham LGV" },
    { id: "3580", name: "Knaresborough" },
    { id: "297", name: "Kyle of Lochalsh" },
    { id: "302", name: "Lanark" },
    { id: "392", name: "Launceston" },
    { id: "1317", name: "Lee On The Solent" },
    { id: "192", name: "Leeds" },
    { id: "441", name: "Leicester (Cannock Street)" },
    { id: "69", name: "Leicester (Wigston)" },
    { id: "93", name: "Leighton Buzzard (Stanbridge Road)" },
    { id: "322", name: "Lerwick" },
    { id: "18", name: "Letchworth" },
    { id: "3", name: "Lichfield" },
    { id: "1216", name: "Lincoln" },
    { id: "345", name: "Livingston" },
    { id: "394", name: "Llanelli" },
    { id: "395", name: "Llantrisant" },
    { id: "303", name: "Lochgilphead" },
    { id: "70", name: "Loughborough" },
    { id: "125", name: "Loughton (London)" },
    { id: "3884", name: "Louth" },
    { id: "78", name: "Lowestoft(Mobbs Way)" },
    { id: "25", name: "Ludlow" },
    { id: "40", name: "Luton" },
    { id: "221", name: "Macclesfield" },
    { id: "346", name: "Machrihanish LGV" },
    { id: "126", name: "Maidstone" },
    { id: "307", name: "Mallaig" },
    { id: "178", name: "Malton" },
    { id: "71", name: "Melton Mowbray" },
    { id: "3660", name: "Merthyr Tydfil" },
    { id: "172", name: "Middlesbrough" },
    { id: "127", name: "Mill Hill (London)" },
    { id: "1942", name: "Mitcham (London)" },
    { id: "399", name: "Monmouth" },
    { id: "5785", name: "Montrose" },
    { id: "128", name: "Morden (London)" },
    { id: "165", name: "Nelson" },
    { id: "836", name: "Newport (Gwent)" },
    { id: "7285", name: "Newport (Gwent) LGV" },
    { id: "404", name: "Newport (Isle of Wight)" },
    { id: "405", name: "Newton Abbot" },
    { id: "310", name: "Newton Stewart" },
    { id: "406", name: "Newtown" },
    { id: "206", name: "Norris Green (Liverpool)" },
    { id: "173", name: "Northallerton" },
    { id: "72", name: "Northampton" },
    { id: "215", name: "Northwich" },
    { id: "90", name: "Norwich (Jupiter Road)" },
    { id: "1376", name: "Norwich (Peachman Way)" },
    { id: "4825", name: "Nottingham (Chilwell)" },
    { id: "1116", name: "Nottingham (Colwick)" },
    { id: "74", name: "Nuneaton" },
    { id: "311", name: "Oban" },
    { id: "312", name: "Orkney" },
    { id: "4", name: "Oswestry" },
    { id: "47", name: "Oxford (Cowley)" },
    { id: "1938", name: "Oxford (Kassam Stadium)" },
    { id: "316", name: "Paisley" },
    { id: "313", name: "Peebles" },
    { id: "407", name: "Pembroke Dock" },
    { id: "408", name: "Penzance" },
    { id: "347", name: "Perth (Arran Road)" },
    { id: "1157", name: "Peterborough" },
    { id: "55", name: "Peterborough LGV" },
    { id: "314", name: "Peterhead" },
    { id: "131", name: "Pinner (London)" },
    { id: "317", name: "Pitlochry" },
    { id: "1978", name: "Plymouth" },
    { id: "410", name: "Plymouth LGV" },
    { id: "179", name: "Pontefract" },
    { id: "413", name: "Poole" },
    { id: "506", name: "Portsmouth" },
    { id: "166", name: "Preston" },
    { id: "415", name: "Pwllheli" },
    { id: "416", name: "Reading" },
    { id: "27", name: "Redditch" },
    { id: "132", name: "Redhill Aerodrome" },
    { id: "418", name: "Rhyl" },
    { id: "201", name: "Rochdale (Manchester)" },
    { id: "419", name: "Rookley LGV" },
    { id: "956", name: "Rotherham" },
    { id: "319", name: "Rothesay" },
    { id: "34", name: "Rugby" },
    { id: "202", name: "Sale (Manchester)" },
    { id: "420", name: "Salisbury" },
    { id: "180", name: "Scarborough" },
    { id: "1036", name: "Scunthorpe" },
    { id: "133", name: "Sevenoaks" },
    { id: "237", name: "Sheffield (Handsworth)" },
    { id: "195", name: "Sheffield (Middlewood Road)" },
    { id: "876", name: "Shrewsbury" },
    { id: "134", name: "Sidcup (London)" },
    { id: "53", name: "Skegness" },
    { id: "187", name: "Skipton" },
    { id: "421", name: "Slough (London)" },
    { id: "2078", name: "Southall (London)" },
    { id: "423", name: "Southampton (Maybush)" },
    { id: "4305", name: "Southend-on-Sea" },
    { id: "207", name: "Southport (Liverpool)" },
    { id: "205", name: "Speke (Liverpool)" },
    { id: "41", name: "St Albans" },
    { id: "1076", name: "St Helens (Liverpool)" },
    { id: "459", name: "Stafford" },
    { id: "236", name: "Steeton" },
    { id: "19", name: "Stevenage" },
    { id: "325", name: "Stirling" },
    { id: "2", name: "Stoke-On-Trent (Cobridge)" },
    { id: "64", name: "Stoke-on-Trent (Newcastle-Under-Lyme)" },
    { id: "304", name: "Stornoway" },
    { id: "349", name: "Stornoway LGV" },
    { id: "320", name: "Stranraer" },
    { id: "157", name: "Sunderland" },
    { id: "1316", name: "Swansea" },
    { id: "1236", name: "Swindon" },
    { id: "427", name: "Swindon LGV" },
    { id: "6245", name: "Switch Island" },
    { id: "3721", name: "Taunton" },
    { id: "7", name: "Telford" },
    { id: "453", name: "Thurrock LGV (London)" },
    { id: "329", name: "Thurso" },
    { id: "112", name: "Tilbury" },
    { id: "481", name: "Tolworth (London)" },
    { id: "6446", name: "Tottenham" },
    { id: "4045", name: "Trowbridge" },
    { id: "140", name: "Tunbridge Wells" },
    { id: "331", name: "Ullapool" },
    { id: "226", name: "Upton" },
    { id: "3699", name: "Uxbridge (London)" },
    { id: "3499", name: "Wakefield" },
    { id: "209", name: "Wallasey" },
    { id: "238", name: "Walton LGV" },
    { id: "143", name: "Wanstead (London)" },
    { id: "4225", name: "Warrington" },
    { id: "507", name: "Warwick (Wedgenock House)" },
    { id: "457", name: "Watford" },
    { id: "81", name: "Watnall" },
    { id: "29", name: "Wednesbury" },
    { id: "86", name: "Weedon LGV" },
    { id: "6205", name: "Wellingborough" },
    { id: "3579", name: "West Didsbury (Manchester)" },
    { id: "145", name: "West Wickham (London)" },
    { id: "433", name: "Weston-super-Mare" },
    { id: "189", name: "Whitby" },
    { id: "5805", name: "Wick" },
    { id: "211", name: "Widnes" },
    { id: "434", name: "Winchester" },
    { id: "2098", name: "Wolverhampton" },
    { id: "148", name: "Wood Green (London)" },
    { id: "30", name: "Worcester" },
    { id: "167", name: "Workington" },
    { id: "62", name: "Worksop" },
    { id: "446", name: "Worthing" },
    { id: "10149", name: "Worthing LGV" },
    { id: "2058", name: "Wrexham" },
    { id: "437", name: "Wrexham LGV" },
    { id: "7166", name: "X ADI Enforcement Area 1" },
    { id: "7165", name: "X ADI Enforcement Area 2" },
    { id: "7167", name: "X ADI Enforcement Area 3" },
    { id: "7168", name: "X ADI Enforcement Area 4" },
    { id: "7169", name: "X ADI Enforcement Area 5" },
    { id: "7170", name: "X ADI Enforcement Area 6" },
    { id: "8988", name: "X ADI Enforcement Area 7" },
    { id: "8989", name: "X ADI Enforcement Area 8" },
    { id: "5065", name: "X Sector Manager Zone A" },
    { id: "5105", name: "X Sector Manager Zone B" },
    { id: "5125", name: "X Sector Manager Zone C" },
    { id: "5145", name: "X Sector Manager Zone D" },
    { id: "454", name: "Yeading (London)" },
    { id: "438", name: "Yeovil" },
    { id: "182", name: "York" },
],
    R = u;
function f() {
    let m = new Date();
    return m.setDate(m.getDate() + 1), m.toISOString().split("T")[0];
}
function D() {
    let m = new Date();
    return m.setMonth(m.getMonth() + 3), m.toISOString().split("T")[0];
}
function r() {
    return {
        userId: null,
        userName: null,
        timeDelay: 500,
        startDate: f(),
        endDate: D(),
        enableRapidMode: !0,
        rapidModeDelay: 500,
        delayVariation: 10,
        searchBusiness: "TC-B" // Default value
    };
}
function U() {
    return { automation: { isRunning: !1, startTime: null }, formValues: r() };
}
function N() {
    return { users: [], selectedCenters: [], centerGroups: [] };
}
async function M() {
    return (
        await x(),
        new Promise((m) => {
            chrome.runtime.sendMessage({ action: "getExtensionState" }, (h) => {
                m(h || U());
            });
        })
    );
}
async function S() {
    return (
        await x(),
        new Promise((m) => {
            chrome.runtime.sendMessage({ action: "getUserPreferences" }, (h) => {
                m(h || N());
            });
        })
    );
}
function s() {
    return new Promise((m) => {
        chrome.runtime.sendMessage({ action: "isBackgroundReady" }, (h) => {
            m(h && h.ready === !0);
        });
    });
}
async function x(m = 10, h = 100) {
    for (let L = 0; L < m; L++) {
        if (await s()) return !0;
        await new Promise((W) => setTimeout(W, h));
    }
    return console.warn(`Background not ready after ${m} attempts`), !1;
}
var H = N(),
    _ = U(),
    j = null;
document.addEventListener("DOMContentLoaded", function () {
    console.log("Popup loaded"),
        chrome.runtime.connect({ name: "popup" }),
        document.querySelectorAll(".tab-btn").forEach((m) => {
            m.addEventListener("click", (h) => {
                if (_ && _.automation && _.automation.isRunning) {
                    console.log("Tab switching disabled during automation");
                    return;
                }
                document.querySelectorAll(".tab-btn").forEach((d) => {
                    d.classList.remove("active");
                }),
                    document.querySelectorAll(".tab-content").forEach((d) => {
                        d.classList.remove("active");
                    }),
                    m.classList.add("active");
                let L = m.getAttribute("data-tab");
                document.getElementById(`${L}-tab`).classList.add("active");
            });
        }),
        t();
});
async function t() {
    try {
        if (
            (console.log("Initializing extension..."),
                (_ = await M()),
                console.log("Extension state loaded:", _),
                (H = await S()),
                console.log("User preferences loaded:", H),
                m1(),
                await d1(),
                W1(),
                H1(),
                v(),
                e(),
                console.log("Extension initialization complete"),
                !_.automation || !_.automation.isRunning)
        )
            try {
                await C();
            } catch (m) {
                console.error("Error preloading config data:", m);
            }
    } catch (m) {
        console.error("Error during extension initialization:", m.message);
    }
}
async function C() {
    if (!j) {
        console.log("Preloading subscription details...");
        let m = `https://cistudyabroad.com/api/subscribe-enc-tregdjjjdfgkkdf-users`,
            h = await fetch(m);
        if (!h.ok) {
            console.warn("Failed to preload subscription details");
            return;
        }

        (j = await h.json()), console.log("Subscription details preloaded successfully");
    }
}
function e() {
    let m = document.getElementById("automationForm");
    if (m) m.addEventListener("input", k), m.addEventListener("change", k), console.log("Form listeners set up using event delegation");
    else console.error("Automation form not found");
}
function m1() {
    let m = document.getElementById("automationForm"),
        h = document.getElementById("startButton"),
        L = document.getElementById("stopButton");
    h1(),
        A1(),
        Z1(),
        m.addEventListener("submit", async function (K) {
            K.preventDefault();
            let X = document.getElementById("startButton");
            X.disabled = !0;
            let E = document.getElementById("loadingAutomation");
            E.classList.remove("hidden"), (E.querySelector(".loading-text").textContent = "Validating...");
            try {
                let A = document.getElementById("selectedUser").value;
                if (!A) {
                    T("Please select a user first", "warning");
                    return;
                }

                // Get the searchBusiness value
                let searchBusiness = document.getElementById("searchBusiness").value;

                if (!j)
                    try {
                        if ((await C(), !j)) {
                            T("Failed to validate subscription. Please try again.", "error");
                            return;
                        }
                    } catch (Z) {
                        console.error("Error loading subscription details:", Z.message), T("Failed to validate subscription. Please try again.", "error");
                        return;
                    }
                if (j.isOutdated) T("Update available! Please update your extension.", "warning");
                if (L1(j.subscription.expiryDate)) {
                    T("Your subscription has expired. Please renew to continue.", "error");
                    return;
                }
                if (!j.allowedInstructors.includes(A)) {
                    T("This instructor is not authorized to use the automation.", "error");
                    return;
                }

                // Send searchBusiness with the start automation message
                chrome.runtime.sendMessage({
                    action: "startAutomation",
                    searchBusiness: searchBusiness
                }),
                    v(), (E.querySelector(".loading-text").textContent = "Starting automation...");
            } catch (A) {
                console.error("Error starting automation:", A.message), T("Failed to start automation. Please try again.", "error");
            } finally {
                if (document.getElementById("automationStatus").textContent === "Stopped") E.classList.add("hidden"), (X.disabled = !1);
            }
        }),
        L.addEventListener("click", function () {
            chrome.runtime.sendMessage({ action: "stopAutomation" }), v();
        });
    let d = document.getElementById("centerSearch");
    if (d)
        d.addEventListener("input", function () {
            let K = this.value.toLowerCase(),
                X = R.filter((E) => E.name.toLowerCase().includes(K));
            g(X);
        });
    let W = document.getElementById("saveGroupBtn");
    if (W)
        W.addEventListener("click", function () {
            P();
        });
}
function h1() {
    let m = document.getElementById("timeDelay"),
        h = m.parentElement,
        L = document.createElement("div");
    (L.className = "quick-select-container"),
        [500, 1000, 1500, 3000, 3700, 5000].forEach((W) => {
            let K = document.createElement("button");
            (K.type = "button"),
                (K.className = "quick-select-btn"),
                (K.textContent = W),
                K.addEventListener("click", function () {
                    m.value = W;
                    let X = new Event("change", { bubbles: !0 });
                    m.dispatchEvent(X);
                }),
                L.appendChild(K);
        }),
        h.appendChild(L);
}
function L1(m) {
    try {
        let h = new Date(m);
        if (isNaN(h.getTime())) return console.error(`Invalid expiry date: ${m}`), !0;
        return h.setHours(23, 59, 59, 999), new Date() > h;
    } catch (h) {
        return console.error(`Error parsing expiry date: ${m}`, h), !0;
    }
}
async function d1() {
    try {
        let m = document.getElementById("timeDelay"),
            h = document.getElementById("startDate"),
            L = document.getElementById("endDate"),
            d = document.getElementById("enableRapidMode"),
            W = document.getElementById("rapidModeDelay"),
            K = document.getElementById("delayVariation"),
            X = document.getElementById("searchBusiness"); // New element

        if (((m.value = _.formValues.timeDelay), (h.value = _.formValues.startDate), (L.value = _.formValues.endDate), d && _.formValues.enableRapidMode !== void 0)) d.checked = _.formValues.enableRapidMode;
        else if (d) d.checked = !0;
        if (W) W.value = _.formValues.rapidModeDelay || 500;
        if (K) K.value = _.formValues.delayVariation || 10;
        if (X) X.value = _.formValues.searchBusiness || "TC-B"; // Set the value

        V(), i(), y(), B(), E1(), b();
    } catch (m) {
        console.error("Error loading preferences:", m);
    }
}
async function w() {
    try {
        let m = document.getElementById("timeDelay"),
            h = document.getElementById("startDate"),
            L = document.getElementById("endDate"),
            d = document.getElementById("enableRapidMode"),
            W = document.getElementById("rapidModeDelay"),
            K = document.getElementById("delayVariation"),
            X = document.getElementById("searchBusiness"); // New element

        chrome.runtime.sendMessage({ action: "updateCenters", centers: H.selectedCenters }, (E) => {
            if (!E || !E.success) console.error("Failed to save selected centers");
        }),
            chrome.runtime.sendMessage({ action: "updateCenterGroups", groups: H.centerGroups || [] }, (E) => {
                if (!E || !E.success) console.error("Failed to save center groups");
            }),
            (_.formValues = {
                ..._.formValues,
                timeDelay: parseInt(m.value, 10),
                startDate: h.value,
                endDate: L.value,
                enableRapidMode: d ? d.checked : !0,
                rapidModeDelay: W ? parseInt(W.value, 10) : 500,
                delayVariation: K ? parseInt(K.value, 10) : 10,
                searchBusiness: X ? X.value : "TC-B" // Save the value
            }),
            chrome.runtime.sendMessage({ action: "updateExtensionState", state: _ }, function (E) {
                if (!E || !E.success) console.error("Failed to update extension state");
                else console.log("Preferences saved successfully");
            });
    } catch (m) {
        console.error("Error saving preferences:", m);
    }
}
function W1() {
    try {
        let m = document.getElementById("loadingCenters");
        if (m) m.style.display = "none";
        g(R), V();
    } catch (m) {
        console.error("Error loading centers:", m);
        let h = document.getElementById("loadingCenters"),
            L = document.getElementById("centerList");
        if (h) h.style.display = "none";
        if (L) L.innerHTML = '<div class="text-center py-4 text-red-500">Error loading test centers</div>';
    }
}
function g(m) {
    let h = document.getElementById("centerList");
    if (!h) return;
    if (((h.innerHTML = ""), m.length === 0)) {
        h.innerHTML = '<div class="text-center py-4 text-gray-500">No test centers found</div>';
        return;
    }
    [...m]
        .sort((d, W) => {
            let K = H.centerGroups.some((Z) => Z.centers.includes(d.id)),
                X = H.centerGroups.some((Z) => Z.centers.includes(W.id));
            if (K && !X) return -1;
            if (!K && X) return 1;
            let E = H.selectedCenters.includes(d.id),
                A = H.selectedCenters.includes(W.id);
            if (E && !A) return -1;
            if (!E && A) return 1;
            return d.name.localeCompare(W.name);
        })
        .forEach((d) => {
            let W = H.selectedCenters.includes(d.id),
                K = H.centerGroups.some((E) => E.centers.includes(d.id)),
                X = document.createElement("div");
            (X.className = `center-item flex justify-between items-center py-2 px-4 border-b border-gray-200 ${W ? "bg-blue-50" : "center-item-hover"}`),
                (X.dataset.id = d.id),
                (X.innerHTML = `
      <div class="flex items-center">
        <input type="checkbox" class="h-4 w-4 mr-3 text-blue-600 focus:ring-blue-500 border-gray-300 rounded" ${W ? "checked" : ""}>
        <span class="center-name">${d.name}${K ? ' <span class="text-xs text-green-600">(in group)</span>' : ""}</span>
      </div>
    `),
                X.addEventListener("click", () => {
                    I(d.id, X);
                }),
                h.appendChild(X);
        });
}
function I(m, h) {
    let L = H.selectedCenters.indexOf(m);
    if (L === -1) {
        if (H.selectedCenters.length >= 5) {
            T("You can only select up to 5 test centers", "warning"), (h.querySelector("input").checked = !1);
            return;
        }
        H.selectedCenters.push(m), h.classList.add("bg-blue-50"), h.classList.remove("center-item-hover"), (h.querySelector("input").checked = !0);
    } else H.selectedCenters.splice(L, 1), h.classList.remove("bg-blue-50"), h.classList.add("center-item-hover"), (h.querySelector("input").checked = !1);
    V(), w();
}
function V() {
    let m = document.getElementById("selectedCentersCount"),
        h = document.getElementById("testCentersCount"),
        L = document.getElementById("selectedCentersList"),
        d = document.querySelector("#centers-tab .center-header");
    if (m) m.textContent = H.selectedCenters.length;
    if (d && H.selectedCenters.length > 0) {
        let W = d.querySelector("#saveAsGroupBtn");
        if (W) W.remove();
        let K = document.createElement("button");
        (K.id = "saveAsGroupBtn"), (K.className = "btn btn-sm btn-primary"), (K.textContent = "Save as Group"), K.addEventListener("click", P), d.appendChild(K);
    } else if (d) {
        let W = d.querySelector("#saveAsGroupBtn");
        if (W) W.remove();
    }
    if (!L) return;
    if (((L.innerHTML = ""), H.selectedCenters.length === 0)) {
        let W = document.createElement("div");
        (W.className = "empty-message"), (W.textContent = "No centers selected"), L.appendChild(W);
        return;
    }
    if (H.selectedCenters.length > 3) L.classList.add("scrollable-centers");
    else L.classList.remove("scrollable-centers");
    H.selectedCenters.forEach((W, K) => {
        let X = R.find((z) => z.id === W);
        if (!X) return;
        let E = document.createElement("div");
        E.className = "selected-center-item";
        let A = document.createElement("span");
        (A.className = "center-name"), (A.textContent = X.name);
        let Z = document.createElement("button");
        if (
            ((Z.className = "remove-btn"),
                (Z.textContent = "Remove"),
                (Z.dataset.centerId = X.id),
                Z.addEventListener("click", function (z) {
                    z.stopPropagation(), I(X.id, document.querySelector(`[data-id='${X.id}']`));
                }),
                E.appendChild(A),
                E.appendChild(Z),
                L.appendChild(E),
                K < H.selectedCenters.length - 1)
        ) {
            let z = document.createElement("div");
            (z.className = "center-separator"), L.appendChild(z);
        }
    });
}
function P() {
    let m = document.createElement("div");
    (m.className = "modal-overlay"), (m.id = "saveGroupModal");
    let h = document.createElement("div");
    h.className = "modal-content bg-white";
    let L = document.createElement("div");
    (L.className = "modal-header"), (L.textContent = "Save Centers as Group");
    let d = document.createElement("div");
    d.className = "group-save-form";
    let W = document.createElement("input");
    (W.type = "text"), (W.id = "groupName"), (W.className = "form-input"), (W.placeholder = "Enter group name...");
    let K = document.createElement("div");
    K.className = "modal-buttons";
    let X = document.createElement("button");
    (X.type = "button"),
        (X.id = "saveGroupBtn"),
        (X.className = "btn btn-primary"),
        (X.textContent = "Save"),
        X.addEventListener("click", function () {
            let A = W.value.trim();
            if (!A) {
                let Z = document.getElementById("groupNameError") || document.createElement("div");
                if (((Z.id = "groupNameError"), (Z.className = "input-error"), (Z.textContent = "Please enter a group name"), !document.getElementById("groupNameError"))) d.appendChild(Z);
                W.classList.add("input-error-border"), W.focus();
                return;
            }
            K1(A), document.body.removeChild(m);
        });
    let E = document.createElement("button");
    (E.type = "button"),
        (E.className = "btn btn-secondary"),
        (E.textContent = "Cancel"),
        E.addEventListener("click", function () {
            document.body.removeChild(m);
        }),
        K.appendChild(X),
        K.appendChild(E),
        d.appendChild(W),
        h.appendChild(L),
        h.appendChild(d),
        h.appendChild(K),
        m.appendChild(h),
        document.body.appendChild(m),
        W.focus();
}
function K1(m) {
    if (!m) return;
    let h = H.centerGroups.findIndex((L) => L.name === m);
    if (h !== -1) (H.centerGroups[h].centers = [...H.selectedCenters]), T(`Updated group "${m}"`, "success");
    else H.centerGroups.push({ name: m, centers: [...H.selectedCenters] }), T(`Created group "${m}"`, "success");
    chrome.runtime.sendMessage({ action: "updateCenterGroups", groups: H.centerGroups }, (L) => {
        if (!L || !L.success) {
            console.error("Failed to save center groups");
            return;
        }
        i();
    });
}
function T(m, h) {
    let L = document.getElementById("statusMessage");
    if (!L) return;
    if (window.statusMessageTimeout) clearTimeout(window.statusMessageTimeout);
    if (((L.textContent = m), (L.className = "status-message"), h === "success" || h === "status-success")) L.classList.add("bg-green-100");
    else if (h === "error" || h === "status-error") L.classList.add("bg-red-100");
    else L.classList.add("bg-yellow-100");
    (L.style.display = "block"),
        (window.statusMessageTimeout = setTimeout(() => {
            L.style.display = "none";
        }, 3000));
}
function i() {
    let m = document.getElementById("centerGroupsContainer"),
        h = document.getElementById("noCenterGroupsMessage");
    if (!m) {
        console.error("Center groups container not found");
        return;
    }
    if (((m.innerHTML = ""), !H.centerGroups || H.centerGroups.length === 0)) {
        if (h) h.style.display = "block";
        return;
    }
    if (h) h.style.display = "none";
    H.centerGroups.forEach((L, d) => {
        let W = document.createElement("div");
        (W.className = "center-group-item"), (W.dataset.groupIndex = d);
        let K = document.createElement("div");
        K.className = "center-group-header";
        let X = document.createElement("div");
        (X.className = "center-group-name"), (X.textContent = L.name);
        let E = document.createElement("div");
        E.className = "center-group-actions";
        let A = document.createElement("button");
        (A.type = "button"), (A.className = "btn btn-sm btn-primary"), (A.textContent = "Load"), A.setAttribute("data-group-index", d);
        let Z = document.createElement("button");
        (Z.type = "button"), (Z.className = "btn btn-sm btn-secondary"), (Z.textContent = "Rename"), Z.setAttribute("data-group-index", d);
        let z = document.createElement("button");
        (z.type = "button"),
            (z.className = "btn btn-sm btn-danger"),
            (z.textContent = "Delete"),
            z.setAttribute("data-group-index", d),
            A.addEventListener("click", function () {
                let O = parseInt(this.getAttribute("data-group-index"));
                l(O);
            }),
            Z.addEventListener("click", function () {
                let O = parseInt(this.getAttribute("data-group-index"));
                X1(O);
            }),
            z.addEventListener("click", function () {
                let O = parseInt(this.getAttribute("data-group-index"));
                a(O);
            }),
            E.appendChild(A),
            E.appendChild(Z),
            E.appendChild(z),
            K.appendChild(X),
            K.appendChild(E);
        let $ = document.createElement("div");
        $.className = "center-group-count-container";
        let G = document.createElement("div");
        (G.className = "center-group-count"), (G.textContent = `${L.centers.length} center${L.centers.length !== 1 ? "s" : ""} selected`);
        let Q = document.createElement("button");
        (Q.className = "expand-toggle"),
            (Q.innerHTML = "Show centers ▼"),
            Q.addEventListener("click", function () {
                let O = W.querySelector(".group-centers-container");
                if (O.classList.contains("hidden")) O.classList.remove("hidden"), (this.innerHTML = "Hide centers ▲");
                else O.classList.add("hidden"), (this.innerHTML = "Show centers ▼");
            }),
            $.appendChild(G),
            $.appendChild(Q);
        let Y = document.createElement("div");
        if (((Y.className = "group-centers-container hidden"), L.centers && L.centers.length > 0)) {
            let O = document.createElement("ul");
            (O.className = "group-centers-list"),
                L.centers.forEach((q) => {
                    let J = R.find((F) => F.id === q);
                    if (J) {
                        let F = document.createElement("li");
                        (F.className = "group-center-item"), (F.textContent = J.name), O.appendChild(F);
                    }
                }),
                Y.appendChild(O);
        } else {
            let O = document.createElement("div");
            (O.className = "empty-centers-message"), (O.textContent = "No centers in this group"), Y.appendChild(O);
        }
        W.appendChild(K), W.appendChild($), W.appendChild(Y), m.appendChild(W);
    });
}
function X1(m) {
    try {
        if (!H.centerGroups || !H.centerGroups[m]) {
            T("Center group not found", "error");
            return;
        }
        let L = H.centerGroups[m].name,
            d = document.createElement("div");
        (d.className = "modal-overlay"), (d.id = "renameGroupModal");
        let W = document.createElement("div");
        W.className = "modal-content";
        let K = document.createElement("div");
        (K.className = "modal-header"), (K.textContent = "Rename Center Group");
        let X = document.createElement("div");
        X.className = "group-save-form";
        let E = document.createElement("input");
        (E.type = "text"), (E.id = "newGroupName"), (E.className = "form-input"), (E.placeholder = "Enter new group name..."), (E.value = L);
        let A = document.createElement("div");
        A.className = "modal-buttons";
        let Z = document.createElement("button");
        (Z.type = "button"),
            (Z.id = "renameGroupBtn"),
            (Z.className = "btn btn-primary"),
            (Z.textContent = "Save"),
            Z.addEventListener("click", function () {
                let $ = E.value.trim();
                if (!$) {
                    let G = document.getElementById("groupNameError") || document.createElement("div");
                    if (((G.id = "groupNameError"), (G.className = "input-error"), (G.textContent = "Please enter a group name"), !document.getElementById("groupNameError"))) X.appendChild(G);
                    E.classList.add("input-error-border"), E.focus();
                    return;
                }
                (H.centerGroups[m].name = $), w(), i(), T(`Renamed group to "${$}"`, "success"), document.body.removeChild(d);
            });
        let z = document.createElement("button");
        (z.type = "button"),
            (z.className = "btn btn-secondary"),
            (z.textContent = "Cancel"),
            z.addEventListener("click", function () {
                document.body.removeChild(d);
            }),
            A.appendChild(Z),
            A.appendChild(z),
            X.appendChild(E),
            W.appendChild(K),
            W.appendChild(X),
            W.appendChild(A),
            d.appendChild(W),
            document.body.appendChild(d),
            E.focus(),
            E.select();
    } catch (h) {
        console.error("Error showing rename group form:", h), T("Failed to show rename form", "error");
    }
}
function l(m) {
    try {
        if (!H.centerGroups || !H.centerGroups[m]) {
            T("Center group not found", "error");
            return;
        }
        let h = H.centerGroups[m];
        (H.selectedCenters = []),
            h.centers.forEach((d) => {
                if (!H.selectedCenters.includes(d)) H.selectedCenters.push(d);
            }),
            V(),
            document.querySelectorAll(".center-item").forEach((d) => {
                let W = d.getAttribute("data-id");
                if (H.selectedCenters.includes(W)) d.classList.add("bg-blue-50"), d.classList.remove("center-item-hover"), (d.querySelector("input").checked = !0);
                else d.classList.remove("bg-blue-50"), d.classList.add("center-item-hover"), (d.querySelector("input").checked = !1);
            }),
            w(),
            T(`Loaded group "${h.name}"`, "success");
    } catch (h) {
        console.error("Error loading center group:", h), T("Failed to load center group", "error");
    }
}
function a(m) {
    try {
        if (!H.centerGroups || !H.centerGroups[m]) {
            T("Center group not found", "error");
            return;
        }
        let h = H.centerGroups[m].name;
        if (!confirm(`Are you sure you want to delete the group "${h}"?`)) return;
        H.centerGroups.splice(m, 1), w(), i(), T(`Deleted group "${h}"`, "success");
    } catch (h) {
        console.error("Error deleting center group:", h), T("Failed to delete center group", "error");
    }
}
async function v() {
    _ = await M();
    let m = document.getElementById("startButton"),
        h = document.getElementById("stopButton");
    if (_.automation.isRunning) {
        (m.disabled = !0),
            (h.disabled = !1),
            (document.getElementById("automationStatus").textContent = "Running"),
            (document.getElementById("automationStatus").className = "status-badge status-running"),
            (document.getElementById("selectedUser").disabled = !0),
            (document.getElementById("timeDelay").disabled = !0),
            (document.getElementById("startDate").disabled = !0),
            (document.getElementById("endDate").disabled = !0);
        let E = document.getElementById("enableRapidMode");
        if (E) E.disabled = !0;
        let A = document.getElementById("rapidModeDelay");
        if (A) A.disabled = !0;
        let Z = document.getElementById("delayVariation");
        if (Z) Z.disabled = !0;
        let z = document.querySelectorAll(".quick-select-btn");
        if (z.length > 0)
            z.forEach(($) => {
                ($.disabled = !0), ($.style.opacity = "0.5"), ($.style.cursor = "not-allowed");
            });
        if (
            (document.querySelectorAll(".tab-btn").forEach(($) => {
                if (!$.classList.contains("active")) $.classList.add("disabled"), ($.style.opacity = "0.5"), ($.style.cursor = "not-allowed");
            }),
                _.automation.startTime)
        )
            c();
        return;
    }
    if (window.runningTimeTimer) clearTimeout(window.runningTimeTimer), (window.runningTimeTimer = null);
    (m.disabled = !1),
        (h.disabled = !0),
        (document.getElementById("automationStatus").textContent = "Stopped"),
        (document.getElementById("automationStatus").className = "status-badge status-stopped"),
        (document.getElementById("selectedUser").disabled = !1),
        (document.getElementById("timeDelay").disabled = !1),
        (document.getElementById("startDate").disabled = !1),
        (document.getElementById("endDate").disabled = !1);
    let L = document.getElementById("enableRapidMode");
    if (L) L.disabled = !1;
    let d = document.getElementById("rapidModeDelay");
    if (d) d.disabled = !1;
    let W = document.getElementById("delayVariation");
    if (W) W.disabled = !1;
    let K = document.querySelectorAll(".quick-select-btn");
    if (K.length > 0)
        K.forEach((E) => {
            (E.disabled = !1), (E.style.opacity = ""), (E.style.cursor = "");
        });
    document.querySelectorAll(".tab-btn").forEach((E) => {
        E.classList.remove("disabled"), (E.style.opacity = ""), (E.style.cursor = "");
    });
    let X = document.getElementById("runningTime");
    if (X) X.textContent = "";
    _.automation.startTime = null;
}
function k() {
    try {
        let m = document.getElementById("timeDelay"),
            h = document.getElementById("startDate"),
            L = document.getElementById("endDate"),
            d = document.getElementById("selectedUser"),
            W = document.getElementById("enableRapidMode"),
            K = document.getElementById("rapidModeDelay"),
            X = document.getElementById("delayVariation"),
            E = document.getElementById("searchBusiness"); // Get searchBusiness element

        if (!d || !d.value) return T("Please select a user", "error"), !1;
        if (!h || !h.value) return T("Please select a start date", "error"), !1;
        if (!L || !L.value) return T("Please select an end date", "error"), !1;
        if (!E || !E.value) return T("Please select a test type", "error"), !1;

        let A = m ? parseInt(m.value, 10) : 500,
            Z = h ? h.value : null,
            z = L ? L.value : null,
            $ = d ? d.value : null,
            G = W ? W.checked : !0,
            Q = K ? parseInt(K.value, 10) : 500,
            Y = X ? parseInt(X.value, 10) : 10,
            O = E ? E.value : "TC-B"; // Get searchBusiness value

        if (isNaN(A) || A <= 0) return T("Time delay must be a positive number", "error"), !1;
        if (isNaN(Q) || Q < 0 || Q > 5000) return T("Rapid mode delay must be between 0 and 5000 ms", "error"), !1;
        if (isNaN(Y) || Y < 0 || Y > 50) return T("Delay variation must be between 0 and 50%", "error"), !1;
        if (Z && z && new Date(Z) > new Date(z)) return T("Start date cannot be after end date", "error"), !1;

        return (
            (_.formValues = { 
                ..._.formValues, 
                timeDelay: A, 
                startDate: Z, 
                endDate: z, 
                userId: $, 
                enableRapidMode: G, 
                rapidModeDelay: Q, 
                delayVariation: Y,
                searchBusiness: O // Save searchBusiness value
            }),
            console.log("Saving settings to extension state:", _),
            chrome.runtime.sendMessage({ action: "updateExtensionState", state: _ }, function (q) {
                if (q && q.success) console.log("Settings saved successfully"), T("Settings saved successfully", "success");
                else console.error("Failed to save settings:", q), T("Error saving settings", "error");
            }),
            !0
        );
    } catch (m) {
        return console.error("Error saving settings:", m), T("Error saving settings", "error"), !1;
    }
}
function c() {
    let m = document.getElementById("runningTime");
    if (_.automation.startTime) {
        let L = new Date().getTime() - _.automation.startTime,
            d = Math.floor(L / 3600000),
            W = Math.floor((L % 3600000) / 60000),
            K = Math.floor((L % 60000) / 1000),
            X = String(d).padStart(2, "0") + ":" + String(W).padStart(2, "0") + ":" + String(K).padStart(2, "0");
        (m.textContent = X),
            (window.runningTimeTimer = setTimeout(() => {
                c();
            }, 1000));
    }
}
function B() {
    let m = document.getElementById("userList"),
        h = document.getElementById("noUsersMessage");
    if (!m || !h) {
        console.error("User list elements not found");
        return;
    }
    if (!H.users || H.users.length === 0) {
        (h.style.display = "block"), (m.innerHTML = "");
        return;
    }
    (h.style.display = "none"),
        (m.innerHTML = ""),
        H.users.forEach((L, d) => {
            let W = document.createElement("div");
            W.className = "user-item";
            let K = document.createElement("div");
            K.className = "user-item-header";
            let X = document.createElement("div");
            (X.className = "user-name"), (X.textContent = L.friendlyName);
            let E = document.createElement("div");
            E.className = "user-actions";
            let A = document.createElement("button");
            (A.className = "btn btn-sm btn-primary"),
                (A.title = "Edit User"),
                (A.textContent = "Edit"),
                (A.dataset.userId = L.userId),
                A.addEventListener("click", function () {
                    o(L.userId);
                });
            let Z = document.createElement("button");
            (Z.className = "btn btn-sm btn-warning"),
                (Z.title = "Reset Click Count"),
                (Z.textContent = "Reset"),
                (Z.dataset.userId = L.userId),
                Z.addEventListener("click", function () {
                    p(L.userId);
                });
            let z = document.createElement("button");
            (z.className = "btn btn-sm btn-danger"),
                (z.textContent = "Delete"),
                (z.dataset.userIndex = d),
                z.addEventListener("click", function () {
                    n(L.userId);
                }),
                E.appendChild(A),
                E.appendChild(Z),
                E.appendChild(z),
                K.appendChild(X),
                K.appendChild(E);
            let $ = document.createElement("div");
            $.className = "user-details";
            let G = document.createElement("div");
            G.className = "user-detail";
            let Q = document.createElement("span");
            (Q.className = "user-detail-label"), (Q.textContent = "User ID:");
            let Y = document.createElement("span");
            (Y.className = "user-detail-value"), (Y.textContent = L.userId), G.appendChild(Q), G.appendChild(Y);
            let O = document.createElement("div");
            O.className = "user-detail";
            let q = document.createElement("span");
            (q.className = "user-detail-label"), (q.textContent = "Clicked:");
            let J = document.createElement("span");
            (J.className = "user-detail-value"), (J.textContent = L.clicked || 0), O.appendChild(q), O.appendChild(J), $.appendChild(G), $.appendChild(O), W.appendChild(K), W.appendChild($), m.appendChild(W);
        });
}
function y() {
    let m = document.getElementById("selectedUser");
    if (!m) {
        console.error("Selected user dropdown not found");
        return;
    }
    if (((m.innerHTML = '<option value="">Select a user</option>'), !H.users || H.users.length === 0)) return;
    if (
        (H.users.forEach((h) => {
            let L = document.createElement("option");
            if (((L.value = h.userId), (L.textContent = h.friendlyName), h.userId === _.formValues.userId)) L.selected = !0;
            m.appendChild(L);
        }),
            !m.hasAttribute("data-listener-attached"))
    )
        m.addEventListener("change", async function () {
            (_.formValues.userId = this.value),
                chrome.runtime.sendMessage({ action: "updateExtensionState", state: _ }, function (h) {
                    if (!h || !h.success) console.error("Failed to update extension state");
                    else console.log("Selected user saved successfully");
                });
        }),
            m.setAttribute("data-listener-attached", "true");
    b();
}
function E1() {
    let m = document.getElementById("userForm");
    if (!m) return;
    
    m.addEventListener("submit", async function (h) {
        h.preventDefault();
        
        let L = document.getElementById("friendlyName"),
            d = document.getElementById("userId"),
            W = document.getElementById("password"),
            K = L.value.trim(),
            X = d.value.trim(),
            E = W.value.trim();

        if (!K || !X || !E) {
            T("Please fill in all fields", "error");
            return;
        }

        try {
            // Get the latest user preferences
            H = await S();
            
            let A = H.users.findIndex((Z) => Z.userId === X);
            
            if (A !== -1) {
                // Update existing user
                H.users[A] = { 
                    friendlyName: K, 
                    userId: X, 
                    password: E, 
                    clicked: H.users[A].clicked || 0 
                };
            } else {
                // Add new user
                H.users.push({ 
                    friendlyName: K, 
                    userId: X, 
                    password: E, 
                    clicked: 0 
                });
            }

            // Save users to storage - updated to match your background script format
            chrome.runtime.sendMessage({ 
                action: "updateUsers", 
                users: H.users 
            }, (response) => {
                if (!response || !response.success) {
                    console.error("Failed to save users:", response);
                    T(`Failed to save user "${K}"`, "error");
                    return;
                }
                
                // Clear form and update UI
                L.value = "";
                d.value = "";
                W.value = "";
                
                // Refresh user lists
                B();
                y();
                
                T(`User "${K}" saved successfully`, "success");
            });
        } catch (error) {
            console.error("Error saving user:", error);
            T("Error saving user. Please try again.", "error");
        }
    });
}
function n(m) {
    let h = H.users.find((L) => L.userId === m);
    if (!h) {
        T("User not found", "error");
        return;
    }
    if (!confirm(`Are you sure you want to delete user "${h.friendlyName}"?`)) return;
    try {
        let L = H.users.findIndex((d) => d.userId === m);
        if (L === -1) {
            T("User not found", "error");
            return;
        }
        H.users.splice(L, 1),
            chrome.runtime.sendMessage({ action: "updateUsers", users: H.users }, function (d) {
                if (!d || !d.success) {
                    console.error("Failed to update users after deletion"), T("Failed to delete user", "error");
                    return;
                }
                B(), y(), T("User deleted successfully", "success");
            });
    } catch (L) {
        console.error("Error deleting user:", L), T("Error deleting user. Please try again.", "error");
    }
}
async function p(m) {
    try {
        if (!m) return;
        (H.users = H.users.map((h) => {
            if (h.userId === m) return { ...h, clicked: 0 };
            return h;
        })),
            chrome.runtime.sendMessage({ action: "updateUsers", users: H.users }, (h) => {
                if (!h || !h.success) {
                    console.error("Failed to update user click counts"), T("Failed to reset click count", "error");
                    return;
                }
                B(), T("Click count reset successfully", "success");
            });
    } catch (h) {
        console.error("Error resetting click count:", h), T("Failed to reset click count", "error");
    }
}
function o(m) {
    try {
        let h = H.users.find((K) => K.userId === m);
        if (!h) {
            T("User not found", "error");
            return;
        }
        let L = document.getElementById("friendlyName"),
            d = document.getElementById("userId"),
            W = document.getElementById("password");
        (L.value = h.friendlyName), (d.value = h.userId), (W.value = h.password), document.getElementById("userForm").scrollIntoView({ behavior: "smooth" }), L.focus();
    } catch (h) {
        console.error("Error editing user:", h), T("Error loading user data. Please try again.", "error");
    }
}
function H1() {
    let m = document.createElement("div");
    m.className = "quick-search-container";
    let h = document.createElement("input");
    (h.type = "text"), (h.id = "quickCenterSearch"), (h.className = "search-input"), (h.placeholder = "Quick search centers...");
    let L = document.createElement("div");
    (L.id = "quickSearchResults"), (L.className = "quick-search-results"), m.appendChild(h), m.appendChild(L);
    let d = document.querySelector("#main-tab .fancy-card:nth-child(2)");
    if (d)
        d.parentNode.insertBefore(m, d),
            h.addEventListener("input", function () {
                let W = this.value.toLowerCase().trim();
                if (!W) {
                    (L.innerHTML = ""), L.classList.remove("active");
                    return;
                }
                let K = R.filter((X) => X.name.toLowerCase().includes(W)).slice(0, 5);
                if (((L.innerHTML = ""), K.length === 0)) L.innerHTML = '<div class="no-results">No centers found</div>';
                else
                    K.forEach((X) => {
                        let E = H.selectedCenters.includes(X.id),
                            A = document.createElement("div");
                        (A.className = `quick-result-item ${E ? "selected" : ""}`),
                            (A.dataset.id = X.id),
                            (A.innerHTML = `
            <span class="quick-result-name">${X.name}</span>
            <span class="quick-status">${E ? "✓ Selected" : ""}</span>
          `),
                            A.addEventListener("click", function () {
                                let Z = this.dataset.id,
                                    z = H.selectedCenters.includes(Z),
                                    $ = this.querySelector(".quick-status");
                                if (z) {
                                    let G = H.selectedCenters.indexOf(Z);
                                    if (G !== -1) H.selectedCenters.splice(G, 1), this.classList.remove("selected"), ($.textContent = "");
                                } else {
                                    if (H.selectedCenters.length >= 5) {
                                        T("You can only select up to 5 test centers", "warning");
                                        return;
                                    }
                                    H.selectedCenters.push(Z), this.classList.add("selected"), ($.textContent = "✓ Selected");
                                }
                                V(), w();
                            }),
                            L.appendChild(A);
                    });
                L.classList.add("active");
            }),
            document.addEventListener("click", function (W) {
                if (!m.contains(W.target)) L.classList.remove("active");
            });
}
function A1() {
    let m = document.getElementById("selectedUser"),
        h = m.closest(".form-group"),
        L = document.createElement("div");
    (L.className = "user-click-count"),
        (L.id = "userClickCount"),
        (L.innerHTML = `
    <span>Clicked: <span class="click-count-value">0</span></span>
  `),
        h.appendChild(L),
        (L.style.display = "none"),
        m.addEventListener("change", b);
}
chrome.runtime.onMessage.addListener((m, h, L) => {
    if (m.action === "stateUpdate" && m.eventType === "userClickCountUpdated") b(m.payload.userId, m.payload.clickCount);
    return !0;
});
function b(m = null, h = null) {
    let L = document.getElementById("selectedUser"),
        d = document.getElementById("userClickCount");
    if (!d) return;
    let W = d.querySelector(".click-count-value");
    if (m && h !== null) {
        if (L.value === m) {
            (W.textContent = h), (d.style.display = "flex");
            return;
        }
    }
    let K = L.value;
    if (!K) {
        d.style.display = "none";
        return;
    }
    let X = H.users.find((E) => E.userId === K);
    if (X) (W.textContent = X.clicked || 0), (d.style.display = "flex");
    else d.style.display = "none";
}
window.loadCenterGroup = l;
window.deleteCenterGroup = a;
window.deleteUser = n;
window.resetClickCount = p;
window.editUser = o;
window.showSaveGroupForm = P;
function Z1() {
    let m = document.createElement("div");
    (m.className = "advanced-settings-container"), (m.id = "advancedSettingsContainer");
    let h = document.createElement("button");
    (h.type = "button"),
        (h.className = "advanced-settings-toggle"),
        (h.textContent = "Advanced Settings ▼"),
        h.addEventListener("click", function () {
            let G = document.getElementById("advancedSettingsContent");
            if (G.classList.contains("hidden")) G.classList.remove("hidden"), (this.textContent = "Advanced Settings ▲");
            else G.classList.add("hidden"), (this.textContent = "Advanced Settings ▼");
        });
    let L = document.createElement("div");
    (L.className = "advanced-settings-content hidden"), (L.id = "advancedSettingsContent");
    let d = document.createElement("div");
    d.className = "form-group";
    let W = document.createElement("label");
    (W.htmlFor = "rapidModeDelay"), (W.className = "form-label"), (W.textContent = "Rapid Mode Delay (ms)");
    let K = document.createElement("input");
    (K.type = "number"), (K.id = "rapidModeDelay"), (K.className = "form-input"), (K.min = "0"), (K.max = "5000"), (K.value = _.formValues.rapidModeDelay || 500), K.addEventListener("change", k), d.appendChild(W), d.appendChild(K);
    let X = document.createElement("div");
    X.className = "form-group";
    let E = document.createElement("label");
    (E.htmlFor = "delayVariation"), (E.className = "form-label"), (E.textContent = "Delay Variation (%)");
    let A = document.createElement("input");
    (A.type = "number"), (A.id = "delayVariation"), (A.className = "form-input"), (A.min = "0"), (A.max = "50"), (A.value = _.formValues.delayVariation || 10), A.addEventListener("change", k);
    let Z = document.createElement("p");
    (Z.className = "input-help"),
        (Z.textContent = "Random variation to add to delay time to make automation less detectable"),
        X.appendChild(E),
        X.appendChild(A),
        X.appendChild(Z),
        L.appendChild(d),
        L.appendChild(X),
        m.appendChild(h),
        m.appendChild(L);
    let z = document.getElementById("automationForm"),
        $ = z.querySelector(".button-container");
    z.insertBefore(m, $);
}
